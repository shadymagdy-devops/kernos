# ─────────────────────────────────────────────
#  Kernos Configuration
#  Edit these to match your environment
# ─────────────────────────────────────────────

CPU_WARN=70       # % CPU usage to warn at
CPU_CRIT=90       # % CPU usage to alert at

RAM_WARN=75
RAM_CRIT=90

DISK_WARN=75
DISK_CRIT=88

BACKUP_DEST="$HOME/kernos_backups"
REPORT_DIR="$HOME/kernos_reports"
LOG_FILE="$HOME/.kernos.log"
